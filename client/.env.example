# URL of the deployed API (no trailing slash). Set this in Netlify → Site settings → Environment variables.
# Leave unset for local development (defaults to http://localhost:4000).
VITE_API_URL=https://your-api.onrender.com
